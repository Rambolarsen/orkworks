# Final coordinator store fix report

- Windows coordinator publication now uses `MoveFileExW` with `MOVEFILE_WRITE_THROUGH` for both first writes and replacements; replacement adds `MOVEFILE_REPLACE_EXISTING`. It no longer uses the shared `ReplaceFileW` path. The coordinator attempts a real parent-directory flush through a directory handle and returns an error if opening or flushing that handle fails. First-write publication does not clobber a raced target.
- Existing-directory and `AlreadyExists` paths repeat the parent-directory sync. Fault-injection tests cover a failed parent sync followed by retry, and the race path. An identical proposal retry also repeats the record-directory sync after an earlier publication barrier may have failed.
- Verification: `cargo fmt --manifest-path crates/orkworksd/Cargo.toml --check`, `cargo build --manifest-path crates/orkworksd/Cargo.toml`, `cargo test --manifest-path crates/orkworksd/Cargo.toml` (1357 passed, 3 ignored), and `git diff --check` passed. Focused coordinator store tests: 6 passed; coordinator tests: 36 passed.
- Windows limitation: no native Windows run was available. An isolated `x86_64-pc-windows-gnu` Rust type-check of the Win32 calls passed; the full cross-target crate check stopped because `x86_64-w64-mingw32-gcc` is missing for `ring` and `libz-sys`. Windows directory flush support is filesystem-dependent; failure is reported instead of acknowledged.
