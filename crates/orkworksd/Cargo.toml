[package]
name = "orkworksd"
version = "0.2.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
serde_path_to_error = "0.1"
tower-http = { version = "0.5", features = ["cors"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
portable-pty = "0.8"
uuid = { version = "1", features = ["v4"] }
notify = { version = "7", features = ["macos_kqueue"] }
chrono = { version = "0.4", features = ["clock"] }
dirs = "5"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }
git2 = "0.19"
sha2 = "0.10"
hex = "0.4"
getrandom = "0.3"
libc = "0.2"
sysinfo = "0.39"
fs2 = "0.4"

tempfile = "3"

[target.'cfg(windows)'.dependencies]
# ReplaceFileW preserves Windows replacement semantics for existing targets;
# MoveFileExW publishes new targets without clobbering a raced creation.
windows-sys = { version = "0.61", features = ["Win32_Storage_FileSystem", "Win32_Security", "Win32_System_JobObjects", "Win32_System_Threading", "Win32_System_Diagnostics_ToolHelp"] }
